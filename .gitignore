# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
# .vercel — project.json is committed intentionally (not sensitive)
# .vercel output dir is ignored
.vercel/output

# Local Netlify CLI state (site link cache, not project source)
.netlify

# Local Supabase CLI state (project link cache, not project source)
supabase/.temp
